package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Polish - Premium M3 Color Palette

// Dark Mode counterpart tokens
val DarkBg = Color(0xFF141218)               // Material 3 Dark theme background
val DarkSurface = Color(0xFF211F26)          // M3 Dark Surface
val DarkSurfaceElevated = Color(0xFF2B2930)  // Slightly lighter dark surface

val EmeraldPrimary = Color(0xFF6750A4)       // Primary theme color (re-mapped to purple for compatibility)
val MintSecondary = Color(0xFFD0BCFF)        // Secondary accent theme color (re-mapped to light lavender)
val BlueTertiary = Color(0xFFEFB8C8)         // Tertiary accent theme color (re-mapped to soft rose)

val TextPrimaryDark = Color(0xFFE6E1E5)
val TextSecondaryDark = Color(0xFF938F99)

// Light Mode counterpart tokens (Primary "Professional Polish" Palette)
val LightBg = Color(0xFFFEF7FF)              // Very light lavender/white
val LightSurface = Color(0xFFFFFFFF)         // Pure white
val PrimaryLight = Color(0xFF6750A4)         // Deep purple
val SecondaryLight = Color(0xFFEADDFF)       // Light purple container
val TertiaryLight = Color(0xFF21005D)        // Intense dark purple text

val TextPrimaryLight = Color(0xFF1D1B20)
val TextSecondaryLight = Color(0xFF49454F)

// Priority Colors (Remapped to match Professional Polish / M3 guidelines)
val PriorityHigh = Color(0xFFB3261E)         // Red warning from HTML schema
val PriorityMedium = Color(0xFFE07A5F)       // Soft modern terracotta orange
val PriorityLow = Color(0xFF6750A4)          // Deep purple
val PriorityCompleted = Color(0xFF6750A4)    // Active theme purple
